#include <stdio.h>
int main(){
    int year;
    printf("");
    scanf("%d", &year);
    if (year%4==0);
    printf("Leap Year");
    else if (year%400==0);
    printf("Leap Year");
    else
    printf("not a Leap Year");
    return 0;
}