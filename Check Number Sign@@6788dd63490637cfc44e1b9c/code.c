#include <stdio.h>
int main(){
    int num;
    scanf("%d", &num);
    if (num=+)
    printf("Positive");
    else if (num=-)
    print("Negative");
    else
    printf("Zero");
    return 0;
}