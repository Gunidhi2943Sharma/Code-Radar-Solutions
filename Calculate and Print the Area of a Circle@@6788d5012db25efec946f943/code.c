#include <stdio.h>
int main(){
    float radius, area;
    printf("");
    scanf("%f", &radius, &area);
    pi=3.14
    area=pi*radius*radius;
    printf("Area: %.2f\n", area);
    return 0;
}